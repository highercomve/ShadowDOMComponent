{
  "name": "shadow-component",
  "version": "0.1.2",
  "description": "This is a test implementation of components with shadow dom and full template rendering of changes of state",
  "main": "index.js",
  "scripts": {
    "build": "browserify source/index.js --standalone Shaco | uglifyjs -c > ./dist/shacho.min.js",
    "watch": "watchify source/index.js -o ./dist/shaco.js --standalone Shaco -v",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/highercomve/shadow-component.git"
  },
  "keywords": [
    "shadow",
    "dom",
    "dom",
    "component"
  ],
  "author": "Sergio Marin",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/highercomve/shadow-component/issues"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ]
    ]
  },
  "homepage": "https://github.com/highercomve/shadow-component#readme",
  "devDependencies": {
    "babel": "^6.5.0",
    "babel-preset-es2015": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "uglifyjs": "^2.4.10",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "incremental-dom": "^0.3.0",
    "webcomponents-lite": "^0.6.0"
  }
}
